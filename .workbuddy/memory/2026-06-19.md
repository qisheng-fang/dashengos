# 2026-06-19 工作日志

## 自定义模型管理页路由修复 (07:39)
- `/settings/models/custom` 显示 "Not Found" — `main.tsx` 路由树缺少 `wsModelsCustom` 定义
- 修复：在 main.tsx 添加 import + createRoute + 路由树注册
- ModelsLayout SUB_TABS 从 3→4 tabs，新增"自定义模型"tab (Wrench 图标)
- TypeScript 编译零错误

## AI 输出太"格式化"修复 (07:43)
- 问题：System Prompt 强制"结构化输出"(摘要→分析→行动项→风险) 导致所有回复像企业报告
- 修改 `system-prompt.ts`:
  - Rule #3 "Structured Output" → "Natural & Direct" (禁止强制格式/emoji堆砌/模板化)
  - 删除 "Output Formats" 整段 (Code/Marketing/Business Analysis/Skill 强制模板)
  - 软化所有 mode 指令 (coding/marketing/analysis/technical/creative) — 去掉箭头链式模板
  - Light prompt 同步改为自然风格
- 修改 `chat.ts` DEFAULT_WELCOME: 去掉 emoji、bullet points、"→"箭头，改为2-3句自然对话
- 后端重编译 + 重启完成

## 对话持久化：SSE 流端点保存到数据库 (07:51)
- 问题：SSE stream 端点注释明确写 `void clientThreadId // SSE 端点不依赖 threadId 做持久化`，对话只在 localStorage
- AI 还跟用户说"不能自动保存"、"记忆仅限当前会话"
- 修改 `chat.ts` stream 端点:
  - 流开始前：`INSERT OR IGNORE INTO sessions` + `INSERT INTO messages (role=USER)`
  - 流结束：累积全部 token 内容 → `INSERT INTO messages (role=ASSISTANT)` + 更新 session token_count
  - 非流式回退路径同样处理
- 新增 API：
  - `GET /api/v1/chat/conversations` — 列出用户所有对话 (按 updated_at DESC, limit 100)
  - `GET /api/v1/chat/conversations/:id` — 查看对话详细消息 (验证 ownership)
- 修改 `system-prompt.ts`: 新增 Rule #4 "对话自动保存"，禁止 AI 说"不能保存"
- TypeScript 编译零错误，后端已重启

## 系统提示词优化对标 WorkBuddy (08:15)
- 目标：更简洁、高信噪比，像 WorkBuddy 那样直接
- 改动 `system-prompt.ts`：
  - BASE_SYSTEM_PROMPT 从 ~130 行精简到 ~90 行（去掉 ~40 行冗余）
  - 去掉 Markdown 工具表格 → 单行列举 16 个工具名（省 ~20 行）
  - "What You ARE/NOT" 从 bullet 列表改为紧凑段落
  - 记忆注入：跨对话记忆从 5 类分段改为 1 个 compact 段落
  - 任务类型微调：从分段描述改为 typeHints 映射表，单行注入
  - 新增 Rule #5："Ask when unclear — if the request lacks critical info, ask before acting"
  - `buildLightSystemPrompt()` 同步精简
- TypeScript 编译零错误 ✅
- 后端重编译 + 重启完成 ✅

## 系统提示词反格式化加强 + 对话持久化修复 (08:20)
### 反格式化加强
- 问题：上一轮只改了 Rule #3 "Natural & Direct"，但模型仍输出 **粗体** + 第一步第二步编号 + 报告式回复
- `system-prompt.ts` Rules 重构：新增 **"Anti-Format: How You MUST NOT Write"** 段（7 条明确禁令）
  - ❌ 禁止 **粗体** 强调
  - ❌ 禁止"第一步/第二步/第三步"编号
  - ❌ 禁止报告式分段
  - ❌ 禁止"原因如下:/具体来说:/值得注意的是:"等 AI 填充词
  - ❌ 禁止 markdown 格式（除非展示代码）
  - 回复要像微信聊天一样打字
- `buildLightSystemPrompt()` 简化：从 7 条原则改为 4 条铁律
- TypeScript 零错误 ✅

### 对话持久化修复
- 问题：前端只依赖 Zustand persist (localStorage)，从来不从后端 API 加载。刷新后 if localStorage 未写入则内容丢失
- 修复 `useAppStore.ts`：
  - 新增 `syncConversationsFromBackend()`：初始化时从后端 GET /api/v1/chat/conversations 拉取
  - 新增 `loadMessagesForConversation(convId)`：懒加载指定对话的完整消息
  - `switchConversation()` 现在自动调用懒加载（如果消息为空）
- 修复 `ChatCopilot.tsx`：
  - 初始化 useEffect 增加 syncConversationsFromBackend 调用
  - 同步完成后检查当前活跃对话是否需要懒加载消息
- 前后端均重新编译 + 重启 ✅